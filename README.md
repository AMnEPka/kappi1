# SSH Script Runner - Система выполнения проверок

Веб-приложение для управления и выполнения SSH/WinRM скриптов на удаленных хостах с полной системой RBAC.

## 🚀 Быстрый старт

### Development режим
```bash
docker-compose up -d
```

### Offline/Production режим
См. [OFFLINE-QUICKSTART.md](./OFFLINE-QUICKSTART.md)

## 📚 Документация

- **[OFFLINE-QUICKSTART.md](./OFFLINE-QUICKSTART.md)** - Быстрая инструкция offline развертывания
- **[OFFLINE-DEPLOYMENT.md](./OFFLINE-DEPLOYMENT.md)** - Подробное руководство offline развертывания  
- **[CHECKLIST-OFFLINE.md](./CHECKLIST-OFFLINE.md)** - Чеклист проверки перед развертыванием

## 🎯 Основные возможности

- ✅ Управление хостами (Linux SSH, Windows WinRM)
- ✅ Библиотека проверок с категориями и системами
- ✅ Создание проектов для группового выполнения
- ✅ Real-time выполнение с отслеживанием прогресса (SSE)
- ✅ История выполнения и экспорт результатов в Excel
- ✅ Полная система RBAC с ролями и разрешениями
- ✅ Управление доступом к проектам

## 🛠 Технологический стек

- **Frontend**: React 19, TailwindCSS, shadcn/ui (Node.js 20)
- **Backend**: FastAPI, Python 3.11
- **Database**: MongoDB 6.0
- **Infrastructure**: Docker, Docker Compose, Nginx (production)

## 📦 Режимы развертывания

### Development
```bash
docker-compose up -d
```
- Hot reload для backend и frontend
- Код монтируется с хоста
- Подходит для разработки

### Offline/Production
```bash
docker-compose -f docker-compose.offline.yml up -d
```
- Полностью автономное развертывание
- Весь код и зависимости в образах
- Production build frontend с nginx
- Не требует интернета

## 🔐 Первый вход

По умолчанию создается администратор:
- **Логин**: `admin`
- **Пароль**: `admin123`

⚠️ Обязательно смените пароль после первого входа!

## 🎭 Роли по умолчанию

1. **Администратор** - полные права
2. **Куратор** - просмотр всех результатов
3. **Исполнитель** - выполнение проектов
4. **Разработчик проверок** - создание проверок и хостов
5. **Менеджер проектов** - создание и управление проектами

## 🌐 Порты

- `3000` - Frontend
- `8001` - Backend API
- `27017` - MongoDB (внутри Docker сети)

## 📊 Структура проекта

```
.
├── backend/              # FastAPI backend
│   ├── server.py        # Основной файл приложения
│   ├── init_db.py       # Инициализация БД
│   ├── Dockerfile       # Production образ
│   └── requirements.txt # Python зависимости
├── frontend/            # React frontend
│   ├── src/
│   ├── Dockerfile       # Development образ
│   ├── Dockerfile.prod  # Production образ
│   └── package.json
├── scripts/             # Утилиты для offline развертывания
│   ├── build-offline.sh
│   ├── save-images.sh
│   └── load-images.sh
├── docker-compose.yml           # Development конфигурация
├── docker-compose.offline.yml  # Offline/Production конфигурация
└── README.md
```

## 🔄 Обновление

### Development
```bash
git pull
docker-compose down
docker-compose up -d --build
```

### Offline/Production
1. Пересоберите образы на машине с интернетом
2. Экспортируйте новые образы
3. Перенесите на целевой хост
4. Импортируйте и перезапустите

## 💾 Резервное копирование

```bash
# Остановка (данные сохраняются в volumes)
docker-compose -f docker-compose.offline.yml down

# Создание backup MongoDB
docker run --rm \
  -v ssh-runner_mongodb_data:/data \
  -v $(pwd)/backups:/backup \
  alpine tar czf /backup/mongodb-$(date +%Y%m%d).tar.gz -C /data .

# Запуск обратно
docker-compose -f docker-compose.offline.yml up -d
```

## 🐛 Troubleshooting

### Порты заняты
```bash
netstat -tulpn | grep -E '3000|8001|27017'
```

### Логи
```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f backend
```

### Очистка
```bash
# Остановка и удаление контейнеров (volumes сохраняются)
docker-compose down

# Полная очистка (УДАЛИТ ВСЕ ДАННЫЕ!)
docker-compose down -v
```

## 📄 Лицензия

Proprietary - Все права защищены

## 🤝 Поддержка

Для вопросов и поддержки обращайтесь к команде разработки
