# 📋 ПЛАН ВНЕДРЕНИЯ ТЕСТИРОВАНИЯ В ПРОЕКТ OSIB AUTOMATION TOOL

## 🎯 ЦЕЛЬ

Создать comprehensive систему тестирования для обеспечения качества кода, обнаружения регрессий и поддержки непрерывной разработки.

**Целевые метрики:**
- Backend coverage: ≥ 80%
- Frontend coverage: ≥ 75%
- Все критические workflows покрыты integration тестами
- Основные пользовательские сценарии покрыты E2E тестами

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ ПРОЕКТА

### Анализ структуры

**Backend (FastAPI):**
- ✅ `pytest` уже установлен в requirements.txt
- ❌ Нет тестовой инфраструктуры
- ❌ Нет тестовых файлов
- ✅ Есть структура: API → Services → Models → Database

**Frontend (React 19):**
- ✅ `react-scripts` с поддержкой тестов
- ❌ Нет тестовых библиотек (Jest, RTL, MSW)
- ❌ Нет тестовых файлов
- ✅ Есть структура: Pages → Components → Contexts → Hooks

**Новый функционал (требует тестирования):**
- ✅ Версионирование processor_script (создание, откат версий)
- ✅ Проверка синтаксиса bash скриптов
- ✅ Группы проверок (check-groups)
- ✅ Планировщик заданий (scheduler)
- ✅ RBAC система (роли и права)

---

## 🗺️ ПЛАН ВНЕДРЕНИЯ (ПО ФАЗАМ)

### PHASE 1: Foundation & Backend Unit Tests (Неделя 1-2)

**Цель:** Создать базовую инфраструктуру и покрыть критичные сервисы unit тестами.

#### 1.1 Настройка инфраструктуры

**Backend:**
- [ ] Создать `backend/tests/` структуру
- [ ] Создать `backend/tests/conftest.py` с fixtures
- [ ] Создать `backend/requirements-test.txt`
- [ ] Создать `backend/pytest.ini`
- [ ] Настроить mongomock для unit tests

**Frontend:**
- [ ] Установить тестовые зависимости (Jest, RTL, MSW)
- [ ] Создать `frontend/src/setupTests.js`
- [ ] Создать `frontend/jest.config.js`
- [ ] Настроить MSW для API mocking

**Приоритетные файлы для создания:**
```
backend/
├── tests/
│   ├── __init__.py
│   ├── conftest.py ★
│   ├── unit/
│   │   ├── __init__.py
│   │   ├── test_auth_service.py ★
│   │   ├── test_rbac_service.py ★
│   │   └── test_validators.py ★
│   └── fixtures/
│       ├── __init__.py
│       └── builders.py ★
├── requirements-test.txt ★
└── pytest.ini ★

frontend/
├── src/
│   ├── __tests__/
│   │   ├── setup.js ★
│   │   └── __mocks__/
│   │       ├── handlers.js ★
│   │       └── mswServer.js ★
└── jest.config.js ★
```

#### 1.2 Unit Tests для критичных сервисов

**Приоритет 1 (Критично):**
- [x] `test_auth_service.py` - JWT, login, password hashing (**30 тестов** ✅)
- [x] `test_rbac_service.py` - Permissions, roles (**15 тестов** ✅)
- [x] `test_validators.py` - Email, hostname, port validation (**30 тестов** ✅)

**Приоритет 2 (Высокий):**
- [x] `test_db_utils.py` - Base64 encoding/decoding, datetime handling (**20 тестов** ✅)
- [x] `test_audit_utils.py` - Audit logging (**8 тестов** ✅)

**Целевое покрытие:** 30-40% backend кода

---

### PHASE 2: Backend Integration Tests (Неделя 3-4)

**Цель:** Покрыть основные workflows через API с реальной БД.

#### 2.1 Настройка Integration тестов

- [ ] Создать `backend/tests/integration/` структуру
- [ ] Настроить testcontainers или docker-compose для MongoDB
- [ ] Создать mock'и для SSH/WinRM (Paramiko, pywinrm)
- [ ] Настроить fixtures для cleanup после тестов

#### 2.2 Integration Tests для workflows

**Приоритет 1:**
- [ ] `test_auth_flow.py` - Login → Token → Verify → Refresh (5+ тестов)
- [ ] `test_host_management_flow.py` - Create → Verify → Update → Delete (6+ тестов)
- [ ] `test_script_workflow.py` - Category → System → Script creation (4+ тестов)

**Приоритет 2:**
- [ ] `test_project_workflow.py` - Create → Configure → Execute → Results (8+ тестов)
- [ ] `test_rbac_integration.py` - Role creation → Permission assignment → Access check (5+ тестов)

**Приоритет 3 (Новый функционал):**
- [ ] `test_processor_script_versioning.py` - Version creation → Rollback → History (6+ тестов)
- [ ] `test_syntax_validation.py` - Bash syntax check endpoint (4+ тестов)
- [ ] `test_check_groups_workflow.py` - Group creation → Assignment → Filtering (5+ тестов)
- [ ] `test_scheduler_workflow.py` - Job creation → Execution → History (6+ тестов)

**Целевое покрытие:** 60-70% backend workflows

---

### PHASE 3: Frontend Unit Tests (Неделя 5)

**Цель:** Покрыть React компоненты и логику unit тестами.

#### 3.1 Компоненты для тестирования

**Приоритет 1:**
- [ ] `LoginPage.test.jsx` - Form validation, API calls, error handling (8+ тестов)
- [ ] `AuthContext.test.jsx` - Login, logout, token management (6+ тестов)
- [ ] `usePermissions.test.js` - Permission checks (5+ тестов)

**Приоритет 2:**
- [ ] `ScriptsPage.test.jsx` - Form submission, versioning UI, syntax check (10+ тестов)
- [ ] `HostsPage.test.jsx` - Host CRUD operations (8+ тестов)
- [ ] `ProjectsPage.test.jsx` - Project list, creation wizard (6+ тестов)

**Приоритет 3:**
- [ ] `AdvancedCodeEditor.test.jsx` - Editor functionality, syntax highlighting (8+ тестов)
- [ ] `SchedulerPage.test.jsx` - Job creation, schedule management (6+ тестов)

**Целевое покрытие:** 70%+ frontend компонентов

---

### PHASE 4: E2E Tests (Неделя 6-7)

**Цель:** Покрыть полные пользовательские сценарии.

#### 4.1 Настройка E2E инфраструктуры

- [ ] Установить Playwright
- [ ] Создать `tests/e2e/` структуру
- [ ] Создать Page Object классы
- [ ] Настроить `docker-compose.test.yml`

#### 4.2 E2E тесты для критичных сценариев

**Приоритет 1:**
- [ ] `test_login_flow.py` - Login → Dashboard navigation (3+ тестов)
- [ ] `test_create_and_execute_project.py` - Full project lifecycle (5+ тестов)
- [ ] `test_rbac_enforcement.py` - Permission-based UI visibility (4+ тестов)

**Приоритет 2 (Новый функционал):**
- [ ] `test_processor_script_versioning.py` - Create version → View history → Rollback (4+ тестов)
- [ ] `test_syntax_check.py` - Syntax validation UI flow (3+ тестов)
- [ ] `test_check_groups.py` - Group creation → Assignment → Filtering (4+ тестов)
- [ ] `test_scheduler_flow.py` - Create job → View history → Execute (5+ тестов)

**Целевое покрытие:** 10-15 основных пользовательских сценариев

---

### PHASE 5: API Contract & Performance Tests (Неделя 8)

**Цель:** Обеспечить стабильность API и производительность.

#### 5.1 API Contract Tests

- [ ] `test_api_contracts.py` - Schema validation для всех endpoints (20+ тестов)
- [ ] Проверка HTTP status codes
- [ ] Проверка response schemas

#### 5.2 Performance Tests

- [ ] `locustfile.py` - Load testing scenarios
- [ ] Тестирование под нагрузкой (100 concurrent users)
- [ ] SLA validation (p95 < 2 сек)

---

### PHASE 6: CI/CD Integration (Неделя 9)

**Цель:** Автоматизировать запуск тестов в CI/CD.

- [ ] Создать `.github/workflows/tests.yml`
- [ ] Настроить параллельное выполнение тестов
- [ ] Настроить coverage reports
- [ ] Настроить артефакты (screenshots, reports)

---

## 🔧 АЛГОРИТМ РАБОТЫ С НОВЫМ ФУНКЦИОНАЛОМ

### Общий подход: Test-Driven Development (TDD) для нового функционала

```
┌─────────────────────────────────────────────────────────┐
│         АЛГОРИТМ ВНЕДРЕНИЯ НОВОГО ФУНКЦИОНАЛА           │
└─────────────────────────────────────────────────────────┘

1️⃣ ПЛАНИРОВАНИЕ
   ├─ Анализ требований
   ├─ Определение API контракта
   ├─ Определение тестовых сценариев
   └─ Создание задачи в трекере

2️⃣ НАПИСАНИЕ ТЕСТОВ (TDD)
   ├─ Unit тесты для бизнес-логики
   ├─ Integration тесты для API endpoints
   ├─ Frontend unit тесты для UI компонентов
   └─ E2E тесты для пользовательских сценариев

3️⃣ РЕАЛИЗАЦИЯ
   ├─ Backend: API → Services → Models
   ├─ Frontend: Pages → Components → Hooks
   └─ Итеративная разработка до прохождения тестов

4️⃣ РЕФАКТОРИНГ
   ├─ Улучшение кода при сохранении покрытия
   ├─ Оптимизация производительности
   └─ Улучшение читаемости

5️⃣ ДОКУМЕНТАЦИЯ
   ├─ Обновление API документации
   ├─ Обновление пользовательской документации
   └─ Добавление примеров использования
```

---

### Конкретный пример: Версионирование processor_script

#### Шаг 1: Планирование

**Требования:**
- Сохранение истории версий processor_script
- Возможность отката к предыдущей версии
- Комментарии к версиям
- SHA-1 хеш для проверки целостности

**API контракт:**
```python
POST /api/scripts/{id}/processor-versions/rollback?version_number={n}
GET /api/scripts/{id}/processor-versions
```

#### Шаг 2: Написание тестов (TDD)

**Unit тесты (`test_processor_script_versioning.py`):**
```python
# 1. Тест создания первой версии
async def test_create_first_version():
    # Arrange
    script_data = {"processor_script": "echo 'test'"}
    
    # Act
    result = prepare_processor_script_version_update(
        script_data={},
        new_content="echo 'test'",
        comment="Первая версия",
        create_new_version=False,
        user_id="user1"
    )
    
    # Assert
    assert result["processor_script_version"]["version_number"] == 1
    assert result["processor_script_version"]["content"] == "echo 'test'"

# 2. Тест создания новой версии
async def test_create_new_version():
    # Arrange
    current_version = {
        "processor_script_version": {
            "content": "echo 'old'",
            "version_number": 1
        }
    }
    
    # Act
    result = prepare_processor_script_version_update(
        script_data=current_version,
        new_content="echo 'new'",
        comment="Обновление",
        create_new_version=True,
        user_id="user1"
    )
    
    # Assert
    assert result["processor_script_version"]["version_number"] == 2
    assert len(result["processor_script_versions"]) == 1  # Старая версия в истории

# 3. Тест отката версии
async def test_rollback_version():
    # ...
```

**Integration тесты:**
```python
# 1. Тест полного workflow версионирования
async def test_versioning_workflow():
    # 1. Создать скрипт с processor_script
    # 2. Обновить processor_script (создать версию 2)
    # 3. Получить список версий
    # 4. Откатить к версии 1
    # 5. Проверить, что текущая версия = версия 1
```

**Frontend unit тесты:**
```javascript
// ScriptsPage.test.jsx
describe('Processor Script Versioning', () => {
  it('should display version history dialog', async () => {
    // ...
  });
  
  it('should allow rollback to previous version', async () => {
    // ...
  });
});
```

**E2E тесты:**
```python
# test_processor_script_versioning.py
async def test_versioning_e2e(page):
    # 1. Login
    # 2. Открыть скрипт для редактирования
    # 3. Изменить processor_script
    # 4. Нажать "Версии"
    # 5. Проверить список версий
    # 6. Откатить к предыдущей версии
    # 7. Проверить, что версия откатилась
```

#### Шаг 3: Реализация

**Backend:**
1. Обновить модель `Script` (добавить `processor_script_version`, `processor_script_versions`)
2. Реализовать `prepare_processor_script_version_update()` в `db_utils.py`
3. Добавить endpoints в `api_scripts.py`:
   - `GET /api/scripts/{id}/processor-versions`
   - `POST /api/scripts/{id}/processor-versions/rollback`
4. Обновить `create_script()` и `update_script()` для работы с версиями

**Frontend:**
1. Обновить `ScriptsPage.jsx`:
   - Добавить кнопку "Версии"
   - Добавить диалог истории версий
   - Добавить функционал отката
2. Обновить форму редактирования для работы с версиями

#### Шаг 4: Рефакторинг

- Оптимизация запросов к БД
- Улучшение UI/UX
- Добавление валидации

#### Шаг 5: Документация

- Обновить API документацию
- Добавить примеры в README
- Обновить пользовательскую документацию

---

### Чеклист для каждого нового функционала

**Перед началом разработки:**
- [ ] Определены требования и API контракт
- [ ] Написаны unit тесты для бизнес-логики
- [ ] Написаны integration тесты для API
- [ ] Написаны frontend unit тесты для UI
- [ ] Написаны E2E тесты для пользовательских сценариев

**Во время разработки:**
- [ ] Тесты проходят после каждого этапа
- [ ] Coverage не снижается
- [ ] Код соответствует стандартам проекта

**После завершения:**
- [ ] Все тесты проходят
- [ ] Coverage ≥ 80% для нового кода
- [ ] Обновлена документация
- [ ] Проведен code review
- [ ] Функционал протестирован вручную

---

## 📈 МЕТРИКИ ПРОГРЕССА

### Неделя 1-2: Foundation
- ✅ Инфраструктура настроена
- ✅ 30-40% backend coverage (unit tests)
- ✅ 0% frontend coverage (инфраструктура готова)

### Неделя 3-4: Backend Integration
- ✅ 60-70% backend coverage (unit + integration)
- ✅ Все критические workflows покрыты

### Неделя 5: Frontend Unit
- ✅ 70%+ frontend coverage
- ✅ Все основные компоненты покрыты

### Неделя 6-7: E2E
- ✅ 10-15 E2E тестов для основных сценариев
- ✅ Page Objects созданы

### Неделя 8: API & Performance
- ✅ API contract tests написаны
- ✅ Performance tests настроены

### Неделя 9: CI/CD
- ✅ GitHub Actions workflow работает
- ✅ Автоматические отчеты настроены

---

## 🚀 БЫСТРЫЙ СТАРТ

### Команды для запуска тестов

```bash
# Backend Unit Tests
cd backend
pytest tests/unit/ -v --cov=app --cov-report=html

# Backend Integration Tests
pytest tests/integration/ -v -m integration

# Frontend Unit Tests
cd frontend
npm test -- --coverage --watchAll=false

# E2E Tests
docker-compose -f docker-compose.test.yml up -d
pytest tests/e2e/ -v --headed

# All Tests
pytest tests/ -v
npm test -- --watchAll=false
```

---

## 📝 ПРИМЕЧАНИЯ

1. **Приоритет тестирования:** Критичные функции (Auth, RBAC) → Основные workflows → Новый функционал → UI компоненты
2. **TDD подход:** Для нового функционала всегда начинать с тестов
3. **Coverage не цель:** 80% хороших тестов лучше 100% плохих
4. **Постепенное внедрение:** Не пытаться покрыть всё сразу, двигаться поэтапно
5. **Регрессионное тестирование:** При каждом изменении запускать релевантные тесты

---

## 🔗 СВЯЗАННЫЕ ДОКУМЕНТЫ

- `tests/testing_plan.md` - Полный стратегический план
- `tests/testing_algorithms.md` - Детальные алгоритмы выполнения
- `tests/testing_configs.md` - Конфигурационные файлы и примеры
- `tests/testing_summary.md` - Краткое резюме

---

**Следующий шаг:** Начать с PHASE 1 - создание базовой инфраструктуры тестирования.
