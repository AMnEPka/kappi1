# Инструмент автоматизации ОСИБ

Веб-приложение для управления и выполнения проверок на удаленных серверах через SSH (Linux) и WinRM (Windows) с иерархической системой организации, планированием заданий, системой ролей и отчетностью.

## 🚀 Быстрый старт

**Через Docker (рекомендуется):**
```bash
git clone <your-repo-url>
cd osib-automation-tool
docker compose up -d --build
```

Откройте http://localhost:3000

📖 **Полная инструкция:** [DOCKER_DEPLOYMENT.md](./DOCKER_DEPLOYMENT.md)  
📖 **Оффлайн установка:** [OFFLINE_SETUP.md](./OFFLINE_SETUP.md)  
📖 **Решение проблем:** [DOCKER_TROUBLESHOOTING.md](./DOCKER_TROUBLESHOOTING.md)  
📖 **Kubernetes развертывание:** [helm/kappi1/README.md](./helm/kappi1/README.md)

---

## ✨ Нововведения текущей версии

### 🔐 Система аутентификации и авторизации (RBAC)
- Полноценная система управления пользователями и ролями
- JWT-аутентификация с безопасным хранением токенов
- Гранулярная система прав доступа (permissions)
- Административные роли с расширенными правами
- Аудит всех действий пользователей

### 📅 Планировщик заданий
- Автоматическое выполнение проектов по расписанию
- Три типа заданий: одноразовое, множественный запуск, повторяющееся
- Гибкая настройка расписания (ежедневно, по времени, по датам)
- История выполнения запланированных заданий
- Автоматический расчет следующего запуска

### 🏷️ Группы проверок
- Организация проверок в логические группы
- Назначение проверок нескольким группам одновременно
- Упрощенная навигация и фильтрация проверок
- Группировка для отчетности и анализа

### ☸️ Развертывание в Kubernetes
- Helm chart для простого развертывания
- Поддержка различных типов сервисов (LoadBalancer, ClusterIP, Ingress)
- Готовые конфигурации для production окружений
- Масштабирование и отказоустойчивость

### 🔒 Улучшения безопасности
- Base64 кодирование содержимого скриптов в базе данных
- Улучшенное шифрование чувствительных данных
- Безопасное хранение паролей и SSH ключей
- Оптимизация CORS настроек

### 🎨 Улучшения интерфейса
- Обновленный логотип (SVG формат)
- Улучшенная поддержка автозаполнения форм
- Исправления компонента выбора даты и времени
- Дополнительные размеры шрифтов для лучшей читаемости

---

## 🎯 Возможности приложения

### ✅ Управление хостами
- Добавление серверов с SSH (Linux) и WinRM (Windows) подключением
- Поддержка аутентификации по паролю и SSH ключу
- Автоматическое определение типа подключения
- Редактирование и удаление хостов
- Предварительная проверка доступности (сеть, логин, sudo/admin)
- Шифрованное хранение учетных данных

### ✅ Иерархическая система проверок
- Организация по **Категориям → Системам → Проверкам**
- Создание проверок с:
  - Командами для выполнения
  - Скриптами-обработчиками (Bash/PowerShell)
  - Эталонными файлами для сравнения
  - Описанием методики испытания
  - Критериями успешного прохождения
  - Порядком отображения
  - Привязкой к группам проверок
- Редактирование и удаление на всех уровнях
- Base64 кодирование для безопасного хранения скриптов

### ✅ Группы проверок
- Создание логических групп для организации проверок
- Назначение проверок одной или нескольким группам
- Упрощенная навигация по связанным проверкам
- Использование групп для фильтрации и отчетности
- Управление группами через API и интерфейс

### ✅ Система проектов
- Создание проектов как шаблонов для многократного выполнения
- Мастер создания проекта (4 шага):
  1. Название и описание
  2. Выбор хостов
  3. Назначение систем и проверок для каждого хоста
  4. Редактирование задач и эталонных данных перед запуском
- Множественные запуски одного проекта с историей сессий
- Управление доступом к проектам на уровне пользователей

### ✅ Планировщик заданий
- **Одноразовое задание** - выполнение проекта в указанное время
- **Множественный запуск** - выполнение по списку дат/времени
- **Повторяющееся задание** - ежедневное выполнение по расписанию
- Автоматический расчет следующего запуска
- История выполнения с результатами
- Управление статусом заданий (активно/приостановлено/завершено)
- Фоновый worker для автоматического выполнения

### ✅ Выполнение и мониторинг
- Выполнение проектов с обратной связью в реальном времени (SSE)
- Оптимизация: одно SSH/WinRM подключение на хост
- Динамический выбор обработчика (Bash/PowerShell) по типу хоста
- Отображение прогресса с детализацией по хостам и проверкам
- Поддержка переменных окружения:
  - `$CHECK_OUTPUT` - вывод команд проверки
  - `$ETALON_INPUT` - содержимое эталонных файлов
- Обработка ошибок и таймаутов

### ✅ Результаты и отчетность
- Просмотр результатов по сессиям выполнения
- Группировка по хостам с детальной статистикой
- 4 статуса проверок: Пройдена, Не пройдена, Ошибка, Оператор
- Детальный просмотр вывода каждой проверки
- **Экспорт результатов в Excel** с форматированием по ГОСТ
- История всех выполненных проектов

### ✅ Система пользователей и ролей (RBAC)
- Управление пользователями (создание, редактирование, деактивация)
- Создание и настройка ролей с набором прав доступа
- Гранулярные разрешения для различных операций:
  - Управление хостами
  - Управление проверками и категориями
  - Управление проектами
  - Выполнение проектов
  - Управление пользователями и ролями
  - Просмотр результатов
- Административные роли с полным доступом
- Отслеживание создателя для всех сущностей
- Система аудита всех действий пользователей

---

## 🛠 Технологии

**Backend:**
- FastAPI - веб-фреймворк
- Motor - асинхронный драйвер MongoDB
- Paramiko - SSH клиент для Linux
- pywinrm - WinRM клиент для Windows
- openpyxl - генерация Excel отчетов
- Cryptography - шифрование паролей
- PyJWT - JWT токены для аутентификации
- Passlib - хеширование паролей

**Frontend:**
- React 19 - UI библиотека
- Tailwind CSS - стилизация
- Shadcn UI - компоненты UI
- Axios - HTTP клиент
- Sonner - уведомления
- React Router - маршрутизация

**Database:**
- MongoDB - хранение данных

**Deployment:**
- Docker & Docker Compose
- Kubernetes & Helm
- Supervisord - управление процессами
- Nginx - reverse proxy (production)

---

## 📖 Использование

### 1. Первый вход и настройка

#### 1.1. Вход в систему
1. Откройте приложение в браузере
2. Введите логин и пароль администратора
3. После успешного входа вы будете перенаправлены на главную страницу

#### 1.2. Управление пользователями (только для администраторов)
1. Перейдите на страницу "Администрирование" → "Пользователи"
2. Создайте новых пользователей или отредактируйте существующих
3. Назначьте роли пользователям для управления доступом

#### 1.3. Управление ролями (только для администраторов)
1. Перейдите на страницу "Администрирование" → "Роли"
2. Создайте роли с необходимым набором прав доступа
3. Назначьте роли пользователям

### 2. Настройка структуры проверок

#### 2.1. Создание категорий
1. Перейдите на страницу "Проверки" → "Категории"
2. Нажмите "Создать категорию"
3. Введите название (например, "Сетевая безопасность")
4. Сохраните

#### 2.2. Создание систем
1. В категории нажмите "Добавить систему"
2. Введите название системы (например, "Firewall")
3. Сохраните

#### 2.3. Создание групп проверок (опционально)
1. Перейдите на страницу "Проверки" → "Группы проверок"
2. Нажмите "Создать группу"
3. Введите название группы (например, "Критические проверки")
4. Сохраните

#### 2.4. Создание проверок
1. В системе нажмите "Добавить проверку"
2. Заполните поля:
   - **Название** - краткое описание проверки
   - **Описание** - детальное описание (опционально)
   - **Команды** - команды для выполнения на хосте
   - **Скрипт-обработчик** - Bash или PowerShell скрипт для анализа результата
   - **Эталонные файлы** (опционально) - данные для сравнения
   - **Описание методики испытания** - как проводится проверка
   - **Критерий успешного прохождения** - условия успеха
   - **Порядок отображения** - числовое значение для сортировки
   - **Группы проверок** - выберите группы, к которым относится проверка
3. Сохраните

### 3. Добавление хостов

1. Перейдите на страницу "Хосты"
2. Нажмите "Добавить хост"
3. Заполните информацию:
   - **Название** - для идентификации
   - **Хост** - IP адрес или FQDN
   - **Порт** - 22 для SSH (Linux), 5985 для WinRM (Windows)
   - **Имя пользователя**
   - **Тип аутентификации** - пароль или SSH ключ
   - **Тип подключения** - SSH (Linux) или WinRM (Windows)
4. Сохраните

### 4. Создание и выполнение проекта

#### 4.1. Создание проекта
1. Перейдите на страницу "Проекты"
2. Нажмите "Создать проект"
3. **Шаг 1**: Введите название и описание проекта
4. **Шаг 2**: Выберите хосты для включения в проект
5. **Шаг 3**: Для каждого хоста назначьте системы и проверки
6. **Шаг 4**: Просмотрите и подтвердите конфигурацию

#### 4.2. Запуск проекта
1. На странице проектов найдите нужный проект
2. Нажмите "Запустить"
3. Отредактируйте эталонные данные при необходимости
4. Нажмите "Начать выполнение"
5. Наблюдайте за прогрессом в реальном времени:
   - Предварительные проверки (сеть, логин, sudo/admin)
   - Выполнение проверок
   - Обработка результатов

### 5. Планирование заданий

#### 5.1. Создание запланированного задания
1. Перейдите на страницу "Планировщик"
2. Нажмите "Создать задание"
3. Выберите проект для выполнения
4. Выберите тип задания:
   - **Одноразовое** - укажите дату и время выполнения
   - **Множественный запуск** - укажите список дат и времени
   - **Повторяющееся** - настройте ежедневное расписание
5. Настройте параметры выполнения
6. Сохраните задание

#### 5.2. Управление заданиями
- Просмотр списка всех заданий
- Редактирование параметров задания
- Активация/деактивация заданий
- Просмотр истории выполнения
- Удаление завершенных заданий

### 6. Просмотр результатов и экспорт

1. После завершения выполнения нажмите "Просмотр результатов"
2. Выберите сессию запуска (если проект выполнялся несколько раз)
3. Просмотрите результаты:
   - Статистика по каждому хосту
   - Детальные результаты каждой проверки
   - Вывод команд и ошибки
4. Нажмите **"Экспорт в Excel"** для создания отчета:
   - Автоматическое форматирование по ГОСТ
   - Структурированная таблица результатов
   - Готовый протокол испытаний

---

## 🔐 Безопасность

### Хранение данных
- Пароли шифруются с использованием Fernet (симметричное шифрование)
- SSH ключи хранятся в зашифрованном виде в базе данных
- Содержимое скриптов кодируется в Base64 перед сохранением
- Используется безопасное SSH соединение через библиотеку Paramiko

### Аутентификация
- JWT токены для сессий пользователей
- Хеширование паролей с использованием bcrypt
- Автоматический выход при истечении токена
- Защита от несанкционированного доступа

### Авторизация
- Гранулярная система прав доступа
- Проверка прав на уровне API
- Ограничение доступа к проектам
- Аудит всех действий пользователей

---

## 📡 API Endpoints

### Аутентификация
- `POST /api/auth/login` - вход в систему
- `GET /api/auth/me` - информация о текущем пользователе
- `GET /api/auth/permissions` - список всех разрешений

### Хосты
- `GET /api/hosts` - получить все хосты
- `POST /api/hosts` - создать хост
- `GET /api/hosts/{id}` - получить хост по ID
- `PUT /api/hosts/{id}` - обновить хост
- `DELETE /api/hosts/{id}` - удалить хост

### Категории
- `GET /api/categories` - получить все категории
- `POST /api/categories` - создать категорию
- `PUT /api/categories/{id}` - обновить категорию
- `DELETE /api/categories/{id}` - удалить категорию

### Системы
- `GET /api/categories/{category_id}/systems` - получить системы категории
- `POST /api/categories/{category_id}/systems` - создать систему
- `PUT /api/systems/{id}` - обновить систему
- `DELETE /api/systems/{id}` - удалить систему

### Проверки
- `GET /api/systems/{system_id}/scripts` - получить проверки системы
- `POST /api/systems/{system_id}/scripts` - создать проверку
- `PUT /api/scripts/{id}` - обновить проверку
- `DELETE /api/scripts/{id}` - удалить проверку

### Группы проверок
- `GET /api/check-groups` - получить все группы проверок
- `POST /api/check-groups` - создать группу проверок
- `GET /api/check-groups/{id}` - получить группу по ID
- `PUT /api/check-groups/{id}` - обновить группу
- `DELETE /api/check-groups/{id}` - удалить группу

### Проекты
- `GET /api/projects` - получить все проекты
- `POST /api/projects` - создать проект
- `GET /api/projects/{id}` - получить проект по ID
- `PUT /api/projects/{id}` - обновить проект
- `DELETE /api/projects/{id}` - удалить проект
- `PUT /api/projects/{id}/tasks/bulk` - обновить задачи проекта
- `POST /api/projects/{id}/execute` - выполнить проект (SSE)
- `GET /api/projects/{id}/sessions` - получить сессии выполнения
- `GET /api/projects/{id}/sessions/{session_id}/executions` - результаты сессии
- `GET /api/projects/{id}/sessions/{session_id}/export-excel` - экспорт в Excel

### Планировщик
- `GET /api/scheduler/jobs` - получить все задания
- `POST /api/scheduler/jobs` - создать задание
- `GET /api/scheduler/jobs/{id}` - получить задание по ID
- `PUT /api/scheduler/jobs/{id}` - обновить задание
- `DELETE /api/scheduler/jobs/{id}` - удалить задание
- `GET /api/scheduler/jobs/{id}/runs` - получить историю выполнения задания

### Пользователи (только для администраторов)
- `GET /api/users` - получить всех пользователей
- `POST /api/users` - создать пользователя
- `GET /api/users/{id}` - получить пользователя по ID
- `PUT /api/users/{id}` - обновить пользователя
- `DELETE /api/users/{id}` - удалить пользователя

### Роли (только для администраторов)
- `GET /api/roles` - получить все роли
- `POST /api/roles` - создать роль
- `GET /api/roles/{id}` - получить роль по ID
- `PUT /api/roles/{id}` - обновить роль
- `DELETE /api/roles/{id}` - удалить роль

---

## ☸️ Развертывание в Kubernetes

Приложение поддерживает развертывание в Kubernetes через Helm chart.

### Быстрый старт

1. **Соберите и загрузите Docker образы:**
```bash
# Backend
cd backend
docker build -f Dockerfile.prod -t <registry>/kappi1-backend:latest .
docker push <registry>/kappi1-backend:latest

# Frontend
cd ../frontend
docker build -f Dockerfile.prod -t <registry>/kappi1-frontend:latest .
docker push <registry>/kappi1-frontend:latest
```

2. **Сгенерируйте секреты:**
```bash
# Ключ шифрования
python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# JWT секрет
openssl rand -base64 32
```

3. **Установите Helm chart:**
```bash
cd helm/kappi1
helm install kappi1 . \
  --set backend.image.repository=<registry>/kappi1-backend \
  --set frontend.image.repository=<registry>/kappi1-frontend \
  --set backend.env.ENCRYPTION_KEY="your-encryption-key" \
  --set backend.env.JWT_SECRET_KEY="your-jwt-secret"
```

📖 **Подробная документация:** [helm/kappi1/README.md](./helm/kappi1/README.md)  
📖 **Быстрый старт:** [helm/kappi1/QUICKSTART.md](./helm/kappi1/QUICKSTART.md)

---

## 💻 Требования к хостам

### Для Linux серверов (SSH):
- Установлен OpenSSH Server (порт 22)
- Bash доступен
- Пользователь имеет права на выполнение команд
- Для проверок требующих повышенных привилегий: sudo без пароля или с паролем

### Для Windows серверов (WinRM):
- Включен и настроен WinRM (порт 5985 или 5986)
- PowerShell доступен
- Пользователь имеет права на выполнение команд
- Для проверок требующих повышенных привилегий: права администратора

#### Настройка WinRM на Windows:
```powershell
# Включить WinRM
Enable-PSRemoting -Force

# Настроить доверенные хосты (для HTTP)
Set-Item WSMan:\localhost\Client\TrustedHosts -Value "*" -Force

# Разрешить базовую аутентификацию
Set-Item WSMan:\localhost\Service\Auth\Basic -Value $true

# Перезапустить службу
Restart-Service WinRM
```

---

## 📝 Примеры проверок

### Пример 1: Проверка версии ядра Linux

**Команды:**
```bash
uname -r
```

**Скрипт-обработчик (Bash):**
```bash
#!/bin/bash
KERNEL_VERSION=$(echo "$CHECK_OUTPUT" | grep -oP '\d+\.\d+')
REQUIRED_VERSION="5.10"

if (( $(echo "$KERNEL_VERSION >= $REQUIRED_VERSION" | bc -l) )); then
    echo "Пройдена"
else
    echo "Не пройдена"
fi
```

### Пример 2: Проверка открытых портов Windows

**Команды (PowerShell):**
```powershell
Get-NetTCPConnection -State Listen | Select-Object LocalPort
```

**Скрипт-обработчик (PowerShell):**
```powershell
$output = $env:CHECK_OUTPUT
$requiredPorts = @(80, 443)
$success = $true

foreach ($port in $requiredPorts) {
    if ($output -notmatch $port) {
        $success = $false
        break
    }
}

if ($success) {
    Write-Output "Пройдена"
} else {
    Write-Output "Не пройдена"
}
```

### Пример 3: Сравнение с эталонным файлом

**Команды:**
```bash
cat /etc/hosts
```

**Эталонные файлы:**
```
127.0.0.1   localhost
::1         localhost
```

**Скрипт-обработчик (Bash):**
```bash
#!/bin/bash
DIFF=$(diff <(echo "$CHECK_OUTPUT") <(echo "$ETALON_INPUT"))

if [ -z "$DIFF" ]; then
    echo "Пройдена"
else
    echo "Не пройдена"
    echo "Различия:"
    echo "$DIFF"
fi
```

---

## 📁 Структура проекта

```
/app/
├── backend/
│   ├── api/                    # API endpoints
│   │   ├── api_auth.py         # Аутентификация
│   │   ├── api_hosts.py        # Управление хостами
│   │   ├── api_categories.py   # Категории и системы
│   │   ├── api_scripts.py      # Проверки
│   │   ├── api_check_groups.py # Группы проверок
│   │   ├── api_projects.py     # Проекты
│   │   ├── api_scheduler.py    # Планировщик
│   │   ├── api_users.py        # Пользователи
│   │   └── api_roles.py        # Роли
│   ├── models/                 # Модели данных
│   ├── services/               # Бизнес-логика
│   ├── scheduler/              # Планировщик заданий
│   ├── utils/                  # Утилиты
│   ├── server.py               # Основное FastAPI приложение
│   └── requirements.txt        # Python зависимости
├── frontend/
│   ├── src/
│   │   ├── pages/              # Страницы приложения
│   │   │   ├── LoginPage.jsx
│   │   │   ├── ScriptsPage.jsx
│   │   │   ├── ProjectsPage.jsx
│   │   │   ├── SchedulerPage.jsx
│   │   │   ├── RolesPage.jsx
│   │   │   └── ...
│   │   ├── components/         # Компоненты
│   │   └── contexts/           # React контексты
│   └── package.json
├── helm/
│   └── kappi1/                 # Helm chart для Kubernetes
├── nginx/                      # Конфигурация Nginx
└── docker-compose.yml          # Docker Compose конфигурация
```

---

## ⚙️ Переменные окружения

### Backend (.env)
```env
MONGO_URL=mongodb://mongo:27017/
ENCRYPTION_KEY=<32-byte Fernet key>
JWT_SECRET_KEY=<секретный ключ для JWT>
```

### Frontend (.env)
```env
REACT_APP_BACKEND_URL=http://localhost:8001
```

---

## 📌 Примечания

### Производительность
- Таймаут SSH/WinRM соединения: 10 секунд
- Таймаут выполнения команды: 60 секунд
- Предварительная проверка хостов: сеть (ping), логин, sudo/admin права
- Выполнение на множественных хостах происходит последовательно
- Одно подключение на хост для всех проверок (оптимизация)

### Безопасность
- Скрипты-обработчики выполняются локально с передачей вывода через переменные окружения
- Содержимое скриптов кодируется в Base64 перед сохранением в БД
- Все операции логируются в системе аудита
- Пароли и SSH ключи хранятся в зашифрованном виде

### Планировщик
- Фоновый worker проверяет задания каждые 30 секунд (настраивается)
- Автоматический расчет следующего запуска для повторяющихся заданий
- История выполнения сохраняется для каждого задания
- Поддержка временных зон UTC

### Интерфейс
- Поддержка автозаполнения для форм логина
- Улучшенный компонент выбора даты и времени
- SVG логотип для лучшего качества отображения
- Адаптивный дизайн для различных размеров экрана

---

## 🐛 Известные ограничения

- Выполнение проектов происходит последовательно (не параллельно)
- Максимальный размер эталонных файлов ограничен
- Планировщик работает только в UTC временной зоне
- История выполнения хранится без автоматической очистки

---

## 🔄 История изменений

### Версия 1.0.0 (текущая)

**Новые возможности:**
- ✅ Система аутентификации и авторизации (RBAC)
- ✅ Планировщик заданий
- ✅ Группы проверок
- ✅ Развертывание в Kubernetes (Helm chart)
- ✅ Base64 кодирование скриптов
- ✅ Улучшения интерфейса

**Улучшения:**
- 🔧 Оптимизация работы с базой данных
- 🔧 Улучшенная обработка ошибок
- 🔧 Исправления компонента выбора даты/времени
- 🔧 Оптимизация CORS настроек
- 🔧 Улучшенная поддержка автозаполнения

**Исправления:**
- 🐛 Исправлена обработка временных зон в планировщике
- 🐛 Исправлена работа с эталонными файлами
- 🐛 Устранены проблемы с кодированием скриптов
- 🐛 Исправлены проблемы с отображением логотипа

---

## 📞 Поддержка

Для вопросов и проблем создавайте issue в репозитории проекта.

---

## 📄 Лицензия

[Указать лицензию проекта]
